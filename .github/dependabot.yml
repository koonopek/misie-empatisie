version: 2
updates:
  # The workflow pins actions to commit SHAs, which only stay current if
  # something bumps them.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
